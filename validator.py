import re

class Validator:
  def __init__(self):
    pass

  def check(self, data):
    i = 0
    while(i < 5)
      if not data[i]:
      msg.append( i+1, "番目のデータが未登録です" )
    i++

    if msg != null
      #msgの中身が空でなかった場合(空白の項目があった場合)、msgを返す
      return msg 

    #空白でなかった場合、項目ごとにバリデーション実施
      addnum_regex = '^[0-9]{3}-[0-9]{4}$'
      matchOBJ = re.match(addnum_regex, data[2])
    
 
